<aside id="sidebar">
    <a class="sidebar-toggle" title="Expand Sidebar"><i class="toggle icon"></i></a>
    <div class="sidebar-top">
        <p><%= __('sidebar.follow') %>:</p>
        <ul class="social-links">
            <% for (var i in theme.customize.social_links) { %>
                <% if (theme.customize.social_links[i]) { %>
                <li>
                    <a class="social-tooltip" title="<%= i %>" href="<%- url_for(theme.customize.social_links[i]) %>" target="_blank">
                        <i class="icon fa fa-<%= i %>"></i>
                    </a>
                </li>
                <% } %>
            <% } %>
        </ul>
    </div>

    <!-- <% if (is_post()) { %>
        <%- partial('post/nav', {post: page}) %>
    <% } %> -->

    <div class="widget-wrap">
      <% if (theme.widgets) { %>
          <% theme.widgets.forEach(function(widget) { %>
              <%- partial('widget/'+widget) %>
          <% }) %>
      <% } %>

      <!-- <% if(is_home()){ %>
        <% if (theme.widgets) { %>
              <% theme.widgets.forEach(function(widget) { %>
                  <%- partial('widget/'+widget) %>
              <% }) %>
          <% } %>
      <% } else { %>
        <%- partial('widget/profile') %>
        <% if (is_category('Teknologi')) { %>
          <div class="widget widget-title">
            <h1>Index</h1><br>
            <% site.categories.findOne({name: 'Teknologi'}).posts.each(function(post){ %>
              <li><a href="<%- url_for(post.path) %>"><%- post.title %></a><br></li>
            <%}) %>
          </div>
        <% } %>
      <% } %> -->
    </div>
</aside>
